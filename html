<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🎂 تولدتسسسسس مبارک ایدا</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --black: #0a0a0f;
            --blue: #4a90d9;
            --red: #e74c6f;
            --green: #2ecc71;
            --purple: #9b59b6;
        }

        body {
            font-family: 'Vazirmatn', sans-serif;
            background: var(--black);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Loading */
        .loading {
            position: fixed;
            inset: 0;
            background: var(--black);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            transition: opacity 0.5s;
        }

        .loading.hide {
            opacity: 0;
            pointer-events: none;
        }

        .heart {
            width: 50px;
            height: 50px;
            position: relative;
            animation: beat 1s infinite;
        }

        .heart::before,
        .heart::after {
            content: '';
            position: absolute;
            width: 50px;
            height: 75px;
            background: linear-gradient(45deg, var(--red), var(--purple));
            border-radius: 50px 50px 0 0;
        }

        .heart::before {
            left: 25px;
            transform: rotate(-45deg);
            transform-origin: 0 100%;
        }

        .heart::after {
            left: 0;
            transform: rotate(45deg);
            transform-origin: 100% 100%;
        }

        @keyframes beat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.2); }
        }

        .loading p {
            margin-top: 30px;
            color: var(--purple);
            font-size: 1rem;
        }

        .dots-loading {
            display: flex;
            gap: 6px;
            margin-top: 15px;
        }

        .dots-loading span {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            animation: dotMove 1.4s infinite;
        }

        .dots-loading span:nth-child(1) { background: var(--red); animation-delay: 0s; }
        .dots-loading span:nth-child(2) { background: var(--blue); animation-delay: 0.2s; }
        .dots-loading span:nth-child(3) { background: var(--green); animation-delay: 0.4s; }
        .dots-loading span:nth-child(4) { background: var(--purple); animation-delay: 0.6s; }

        @keyframes dotMove {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        /* Stars */
        .stars {
            position: fixed;
            inset: 0;
            pointer-events: none;
        }

        .star {
            position: absolute;
            width: 2px;
            height: 2px;
            background: #fff;
            border-radius: 50%;
            animation: twinkle 3s infinite;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.3; }
            50% { opacity: 1; }
        }

        /* Particles */
        .particles {
            position: fixed;
            inset: 0;
            pointer-events: none;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            border-radius: 50%;
            animation: floatUp 15s linear infinite;
            opacity: 0.5;
        }

        @keyframes floatUp {
            0% { transform: translateY(100vh) rotate(0deg); opacity: 0; }
            10% { opacity: 0.5; }
            90% { opacity: 0.5; }
            100% { transform: translateY(-100px) rotate(360deg); opacity: 0; }
        }

        /* Confetti */
        .confetti {
            position: fixed;
            width: 8px;
            height: 8px;
            top: -10px;
            animation: confettiFall 5s linear infinite;
            z-index: 100;
        }

        @keyframes confettiFall {
            0% { transform: translateY(0) rotate(0deg); opacity: 1; }
            100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
        }

        /* Pages */
        .page {
            display: none;
            min-height: 100vh;
            padding: 40px 20px;
            animation: fadeIn 0.5s ease;
        }

        .page.active {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Card */
        .card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 25px;
            padding: 40px 30px;
            max-width: 600px;
            width: 100%;
            text-align: center;
        }

        .cake {
            font-size: 80px;
            animation: bounce 2s infinite;
            display: block;
            margin-bottom: 20px;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        .title {
            font-size: 2rem;
            font-weight: 900;
            background: linear-gradient(45deg, var(--red), var(--purple), var(--blue));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 15px;
        }

        .name {
            font-size: 2rem;
            font-weight: 900;
            background: linear-gradient(45deg, var(--red), var(--purple), var(--blue), var(--green));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            background-size: 200% auto;
            animation: gradientMove 3s infinite;
        }

        @keyframes gradientMove {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .line {
            width: 80px;
            height: 3px;
            background: linear-gradient(90deg, var(--red), var(--purple), var(--blue));
            margin: 15px auto;
            border-radius: 3px;
        }

        .text {
            font-size: 1.1rem;
            line-height: 2;
            color: rgba(255, 255, 255, 0.9);
            margin: 15px 0;
        }

        .emojis {
            font-size: 1.8rem;
            margin: 15px 0;
            animation: wiggle 2s infinite;
        }

        @keyframes wiggle {
            0%, 100% { transform: rotate(-3deg); }
            50% { transform: rotate(3deg); }
        }

        .red { color: var(--red); font-weight: 700; }
        .blue { color: var(--blue); font-weight: 700; }
        .green { color: var(--green); font-weight: 700; }
        .purple { color: var(--purple); font-weight: 700; }

        /* Love Bar */
        .love-bar {
            display: flex;
            justify-content: center;
            gap: 5px;
            margin: 20px 0;
        }

        .love-bar span {
            width: 16px;
            height: 16px;
            border-radius: 50%;
            animation: pulse 1s infinite;
        }

        .love-bar span:nth-child(1) { background: var(--red); animation-delay: 0s; }
        .love-bar span:nth-child(2) { background: var(--purple); animation-delay: 0.1s; }
        .love-bar span:nth-child(3) { background: var(--blue); animation-delay: 0.2s; }
        .love-bar span:nth-child(4) { background: var(--green); animation-delay: 0.3s; }
        .love-bar span:nth-child(5) { background: var(--red); animation-delay: 0.4s; }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.4); }
        }

        /* Link Box */
        .link-box {
            background: linear-gradient(135deg, rgba(74, 144, 217, 0.2), rgba(155, 89, 182, 0.2));
            border: 2px solid var(--blue);
            border-radius: 20px;
            padding: 25px;
            margin: 20px 0;
            transition: all 0.3s;
        }

        .link-box:hover {
            transform: scale(1.02);
            box-shadow: 0 10px 30px rgba(74, 144, 217, 0.3);
        }

        .link-box.song {
            background: linear-gradient(135deg, rgba(231, 76, 111, 0.2), rgba(46, 204, 113, 0.2));
            border-color: var(--red);
        }

        .link-box.song:hover {
            box-shadow: 0 10px 30px rgba(231, 76, 111, 0.3);
        }

        .link-icon {
            font-size: 50px;
            margin-bottom: 10px;
        }

        .link-title {
            font-size: 1.1rem;
            color: var(--blue);
            margin-bottom: 12px;
        }

        .link-box.song .link-title {
            color: var(--red);
        }

        .link-btn {
            display: inline-block;
            padding: 12px 30px;
            background: linear-gradient(45deg, var(--blue), var(--purple));
            color: #fff;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 700;
            transition: all 0.3s;
        }

        .link-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(74, 144, 217, 0.4);
        }

        .link-box.song .link-btn {
            background: linear-gradient(45deg, var(--red), var(--green));
        }

        .link-box.song .link-btn:hover {
            box-shadow: 0 8px 25px rgba(231, 76, 111, 0.4);
        }

        /* Buttons */
        .nav {
            display: flex;
            gap: 12px;
            margin-top: 30px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .btn {
            padding: 14px 35px;
            font-size: 1rem;
            font-family: inherit;
            font-weight: 700;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-next {
            background: linear-gradient(45deg, var(--purple), var(--blue));
            color: #fff;
        }

        .btn-next:hover {
            transform: translateY(-4px);
            box-shadow: 0 10px 30px rgba(155, 89, 182, 0.4);
        }

        .btn-prev {
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            border: 2px solid rgba(255, 255, 255, 0.2);
        }

        .btn-prev:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-4px);
        }

        /* Dots */
        .dots {
            display: flex;
            gap: 10px;
            margin-top: 25px;
        }

        .dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            cursor: pointer;
            transition: all 0.3s;
        }

        .dot.active {
            background: var(--purple);
            transform: scale(1.3);
            box-shadow: 0 0 15px var(--purple);
        }

        /* Float Heart */
        .float-heart {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 24px;
            animation: floatHeart 3s infinite;
            z-index: 100;
        }

        @keyframes floatHeart {
            0%, 100% { transform: translateX(-50%) translateY(0); }
            50% { transform: translateX(-50%) translateY(-15px); }
        }

        /* Responsive */
        @media (max-width: 480px) {
            .card {
                padding: 25px 18px;
                border-radius: 20px;
            }

            .cake { font-size: 60px; }
            .title { font-size: 1.6rem; }
            .name { font-size: 1.5rem; }
            .text { font-size: 1rem; }
            .emojis { font-size: 1.4rem; }
            .btn { padding: 12px 28px; font-size: 0.95rem; }
            .link-box { padding: 20px 15px; }
            .link-icon { font-size: 40px; }
        }

        @media (min-width: 768px) {
            .card {
                padding: 50px 40px;
                border-radius: 30px;
            }

            .cake { font-size: 100px; }
            .title { font-size: 2.5rem; }
            .name { font-size: 2.2rem; }
            .text { font-size: 1.15rem; }
            .emojis { font-size: 2rem; }
            .btn { padding: 16px 45px; font-size: 1.1rem; }
        }
    </style>
</head>
<body>
    <!-- Loading -->
    <div class="loading" id="loading">
        <div class="heart"></div>
        <p>داره آماده میشه...</p>
        <div class="dots-loading">
            <span></span><span></span><span></span><span></span>
        </div>
    </div>

    <!-- Stars -->
    <div class="stars" id="stars"></div>

    <!-- Particles -->
    <div class="particles" id="particles"></div>

    <!-- Confetti -->
    <div id="confetti"></div>

    <!-- Page 1 -->
    <div class="page active" id="page1">
        <div class="card">
            <span class="cake">🎂</span>
            <h1 class="title">تولدت مبارک!</h1>
            <div class="line"></div>
            <h2 class="name">ایدا عزیزم</h2>
            <div class="line"></div>
            <p class="text">
                امروز روز خاصیه...
                <br>
                روزی که دنیا قشنگ‌تر شد چون <span class="red">تو</span> اومدی 🌸
            </p>
            <div class="emojis">🎉🎈🎁🎊🥳</div>
            <div class="love-bar">
                <span></span><span></span><span></span><span></span><span></span>
            </div>
        </div>
        <div class="nav">
            <button class="btn btn-next" onclick="goPage(2)">صفحه بعد ⟵</button>
        </div>
        <div class="dots">
            <div class="dot active" onclick="goPage(1)"></div>
            <div class="dot" onclick="goPage(2)"></div>
            <div class="dot" onclick="goPage(3)"></div>
            <div class="dot" onclick="goPage(4)"></div>
            <div class="dot" onclick="goPage(5)"></div>
        </div>
    </div>

    <!-- Page 2 -->
    <div class="page" id="page2">
        <div class="card">
            <h1 class="title">یه چند کلمه...</h1>
            <div class="line"></div>
            <p class="text">
                می‌خوام یه چیزایی بگم که شاید همیشه نمیگم
                <br><br>
                تو یه آدم <span class="purple">خاص</span> هستی
                <br>
                کسی که وقتی لبخند می‌زنه، دنیا قشنگ‌تر میشه
                <br><br>
                هر روزت پر از <span class="blue">شادی</span> و <span class="green">موفقیت</span> باشه
            </p>
            <div class="emojis">💜💙💚</div>
        </div>
        <div class="nav">
            <button class="btn btn-prev" onclick="goPage(1)">→ صفحه قبل</button>
            <button class="btn btn-next" onclick="goPage(3)">صفحه بعد ⟵</button>
        </div>
        <div class="dots">
            <div class="dot" onclick="goPage(1)"></div>
            <div class="dot active" onclick="goPage(2)"></div>
            <div class="dot" onclick="goPage(3)"></div>
            <div class="dot" onclick="goPage(4)"></div>
            <div class="dot" onclick="goPage(5)"></div>
        </div>
    </div>

    <!-- Page 3 -->
    <div class="page" id="page3">
        <div class="card">
            <h1 class="title">همیشه کنارتم 💕</h1>
            <div class="line"></div>
            <p class="text">
                می‌خوام بدونی که <span class="red">همیشه</span> کنارتم
                <br>
                تو روزای خوب و روزای بد
                <br>
                تو شادیا و غما
                <br><br>
                هر وقت خواستی، من اینجام
                <br>
                یه پیام، یه زنگ، هر چی...
                <br><br>
                <span class="green">تو تنها نیستی</span>، هرگز
            </p>
            <div class="emojis">🤝💪❤️</div>
        </div>
        <div class="nav">
            <button class="btn btn-prev" onclick="goPage(2)">→ صفحه قبل</button>
            <button class="btn btn-next" onclick="goPage(4)">صفحه بعد ⟵</button>
        </div>
        <div class="dots">
            <div class="dot" onclick="goPage(1)"></div>
            <div class="dot" onclick="goPage(2)"></div>
            <div class="dot active" onclick="goPage(3)"></div>
            <div class="dot" onclick="goPage(4)"></div>
            <div class="dot" onclick="goPage(5)"></div>
        </div>
    </div>

    <!-- Page 4 -->
    <div class="page" id="page4">
        <div class="card">
            <h1 class="title">ببخشید... 🤍</h1>
            <div class="line"></div>
            <p class="text">
                اگه گاهی وقتا ناراحتت کردم
                <br>
                اگه حرفی زدم که دلخور شدی
                <br>
                اگه کاری کردم که آزرده شدی
                <br><br>
                <span class="red">حلالم کن</span> 🙏
                <br><br>
                آدم کامل نیستیم
                <br>
                ولی <span class="purple">دوستت دارم</span>
                <br>
                و <span class="green">ارزشت</span> رو خیلی میدونم
            </p>
            <div class="emojis">😢🙏💐</div>
        </div>
        <div class="nav">
            <button class="btn btn-prev" onclick="goPage(3)">→ صفحه قبل</button>
            <button class="btn btn-next" onclick="goPage(5)">صفحه بعد ⟵</button>
        </div>
        <div class="dots">
            <div class="dot" onclick="goPage(1)"></div>
            <div class="dot" onclick="goPage(2)"></div>
            <div class="dot" onclick="goPage(3)"></div>
            <div class="dot active" onclick="goPage(4)"></div>
            <div class="dot" onclick="goPage(5)"></div>
        </div>
    </div>

    <!-- Page 5 -->
    <div class="page" id="page5">
        <div class="card">
            <h1 class="title">یه هدیه برات! 🎬</h1>
            <div class="line"></div>
            <p class="text">
                یه فیلم انتخاب کردم برات
                <br>
                امیدوارم خوشت بیاد
                <br>
                یه فیلم قشنگ با یه پیام قشنگ 💫
            </p>

            <div class="link-box">
                <div class="link-icon">🎬</div>
                <div class="link-title">فیلم مورد علاقه‌ات</div>
                <a href="https://www.youtube.com/results?search_query=best+movie" 
                   target="_blank" 
                   class="link-btn">
                    تماشای فیلم 🎥
                </a>
            </div>

            <div class="line"></div>
            <p class="text">
                و یه آهنگ قشنگ هم برات دارم 🎵
            </p>

            <div class="link-box song">
                <div class="link-icon">🎵</div>
                <div class="link-title">آهنگ مخصوص تولدت</div>
                <a href="https://irsv.upmusics.com/AliBZ/Arabic%20Happy%20Birthday%20(320).mp3" 
                   target="_blank" 
                   class="link-btn">
                    گوش بده 🎧
                </a>
            </div>

            <div class="line"></div>
            <p class="text">
                <span class="red">دوستت دارم</span> ایدا
                <br>
                تولدت <span class="purple">مبارک</span> 🌸
            </p>
            <div class="emojis">🎂🎉💝</div>
            <div class="love-bar">
                <span></span><span></span><span></span><span></span><span></span>
            </div>
        </div>
        <div class="nav">
            <button class="btn btn-prev" onclick="goPage(4)">→ صفحه قبل</button>
        </div>
        <div class="dots">
            <div class="dot" onclick="goPage(1)"></div>
            <div class="dot" onclick="goPage(2)"></div>
            <div class="dot" onclick="goPage(3)"></div>
            <div class="dot" onclick="goPage(4)"></div>
            <div class="dot active" onclick="goPage(5)"></div>
        </div>
    </div>

    <div class="float-heart">💕</div>

    <script>
        function goPage(num) {
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
            document.getElementById('page' + num).classList.add('active');
            
            document.querySelectorAll('.dot').forEach((d, i) => {
                d.classList.toggle('active', i === num - 1);
            });

            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        window.addEventListener('load', function() {
            setTimeout(function() {
                document.getElementById('loading').classList.add('hide');
            }, 2000);
        });

        var starContainer = document.getElementById('stars');
        for (var i = 0; i < 80; i++) {
            var star = document.createElement('div');
            star.className = 'star';
            star.style.left = Math.random() * 100 + '%';
            star.style.top = Math.random() * 100 + '%';
            star.style.animationDelay = Math.random() * 3 + 's';
            star.style.animationDuration = (Math.random() * 2 + 2) + 's';
            starContainer.appendChild(star);
        }

        var particleContainer = document.getElementById('particles');
        var colors = ['#e74c6f', '#4a90d9', '#2ecc71', '#9b59b6'];
        for (var j = 0; j < 25; j++) {
            var p = document.createElement('div');
            p.className = 'particle';
            p.style.left = Math.random() * 100 + '%';
            var size = Math.random() * 8 + 4;
            p.style.width = size + 'px';
            p.style.height = size + 'px';
            p.style.background = colors[Math.floor(Math.random() * colors.length)];
            p.style.animationDelay = Math.random() * 15 + 's';
            p.style.animationDuration = (Math.random() * 10 + 10) + 's';
            particleContainer.appendChild(p);
        }

        var confettiContainer = document.getElementById('confetti');
        var confettiColors = ['#e74c6f', '#4a90d9', '#2ecc71', '#9b59b6', '#f39c12'];
        for (var k = 0; k < 40; k++) {
            var c = document.createElement('div');
            c.className = 'confetti';
            c.style.left = Math.random() * 100 + '%';
            c.style.background = confettiColors[Math.floor(Math.random() * confettiColors.length)];
            c.style.animationDelay = Math.random() * 5 + 's';
            c.style.animationDuration = (Math.random() * 3 + 3) + 's';
            confettiContainer.appendChild(c);
        }
    </script>
</body>
</html>
